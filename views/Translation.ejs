<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vocabulary Website</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #007BFF;
            color: #fff;
            padding: 20px;
            text-align: center;
        }

        h1 {
            font-size: 36px;
            margin-bottom: 10px;
        }

        .search-container {
            display: flex;
            justify-content: center;
            margin-bottom: 20px;
        }

        .search-input {
            width: 70%;
            padding: 10px;
            font-size: 18px;
            border: none;
        }

        .search-button {
            background-color: #fff;
            color: #007BFF;
            border: none;
            padding: 10px 20px;
            font-size: 18px;
            cursor: pointer;
        }

        nav {
            background-color: #333;
            color: #fff;
            padding: 10px 0;
        }

        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        nav li {
            display: inline;
            margin-right: 20px;
        }

        main {
            max-width: 800px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.2);
        }

        footer {
            background-color: #333;
            color: #fff;
            padding: 10px 0;
            text-align: center;
            margin-top: 10rem;
        }

        /* Translation Form Styles */
        .translation-form {
            text-align: center;
        }

        .translation-form input[type="text"] {
            width: 70%;
            padding: 10px;
            font-size: 18px;
            border: none;
        }

        .translation-form button {
            background-color: #fff;
            color: #007BFF;
            border: none;
            padding: 10px 20px;
            font-size: 18px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <header>
        <h1>Vocabulary Website("Rohit Sharma")</h1>
        <p>Expand your vocabulary and improve your language skills.</p>
        <div class="search-container">
            <form action="/submit" method="post">
                <input type="text" name="vocabulary" class="search-input" placeholder="Search for a word">
                <br><br>
                <button class="search-button">Search</button>
            </form>
        </div>
    </header>

    <nav>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/translate">Dictionary</a></li>
            <li><a href="/word-of-the-day">Word of the Day</a></li>
            <li><a href="/Practice">Practice</a></li>
            <li><a href="/Aboutus">About Us</a></li>
        </ul>
    </nav>

    <main>
        <% if (typeof word !== 'undefined') { %>
            <h2>Featured Word</h2>
            <p><strong>Word:</strong> <%= word %></p>
            <p><strong>Phonetic:</strong> <%= phonetic %></p>
            <p><strong>Definitions:</strong></p>
            <ul>
                <% for (let i = 0; i < meanings.length; i++) { %>
                    <li><%= meanings[i] %></li>
                <% } %>
            </ul>
            <% if (pronunciation) { %>
                <p><strong>Pronunciation:</strong></p>
                <audio controls>
                    <source src="<%= pronunciation %>" type="audio/mpeg">
                    Your browser does not support the audio element.
                </audio>
            <% } %>
        <% } else { %>
            <p>No word found or an error occurred.</p>
        <% } %>

        <!-- Translation Form -->
        <div class="translation-form">
            <h2>Translation</h2>
            <form action="/translate" method="post">
                <input type="text" name="sentence" placeholder="Enter your sentence">
                <button type="submit">Translate</button>
            </form>
            
            <!-- Display Translation Result -->
            <% if (typeof translation !== 'undefined') { %>
                <p><strong>Original Sentence:</strong> <%= originalSentence %></p>
                <p><strong>Translated Sentence:</strong> <%= translation %></p>
            <% } %>

            <!-- Display Translation Error -->
            <% if (typeof error !== 'undefined') { %>
                <p style="color: red;"><%= error %></p>
            <% } %>
        </div>
    </main>

    <footer>
        &copy; 2023 Vocabulary Website
    </footer>
</body>
</html>
